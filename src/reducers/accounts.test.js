import reducer from './accounts';

test('accounts sets its default state', () => {
  expect(reducer()).toEqual([]);
});

test('accounts adds a new account', () => {});

test('accounts updates an existing account', () => {});

test('accounts deletes an account', () => {});

test('accounts adds a contact to an account', () => {});

test('accounts removes a contact from an account', () => {});
